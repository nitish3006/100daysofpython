# Split string method
names_string = input("Give me everybody's names, separated by a comma. ")
names = names_string.split(", ")
# 🚨 Don't change the code above 👆

#Write your code below this line 👇
import random 
num_items = len(names)

random_chances = random.randint(0, num_items - 1)

payee = names[random_chances]

print("person who will pay the bill is", payee)
